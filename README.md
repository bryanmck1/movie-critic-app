# README

Run using the 'bin/dev' command.

To add a new user with admin functionality:
-Sign up for a new user account
-Open the project in the IRB console

- $ user = User.last
- $ user.role = 1
- $ user.save
- The user profile you created is now an admin
